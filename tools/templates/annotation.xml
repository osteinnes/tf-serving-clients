<annotation>
    <folder>{{ folder }}</folder>
    <path>{{ path }}</path>
{% for object in objects %}    <object>
        <filename>{{ object.filename }}</filename>
        <data>
            <num_label_cracks>{{ object.num_label_cracks }}</num_label_cracks>
            <num_pred_cracks>{{ object.num_pred_cracks }}</num_pred_cracks>
                       
                <result>
                    {% for iou in object.ious %} 
                    <iou_score>
                    {{ iou }}
                    </iou_score>
                    {% endfor %}
                    {% for certainty in object.certainties %} 
                    <certainty>
                    {{ certainty }}
                    </certainty>
                    {% endfor %}
                    
                </result>
        </data>
    </object>{% endfor %}
</annotation>
